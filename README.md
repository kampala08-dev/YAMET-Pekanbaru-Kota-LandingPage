# Here are your Instructions
/app
├── backend/         # FastAPI + MongoDB
│   ├── server.py    # POST/GET /api/leads + validasi
│   ├── .env         # env vars (jangan commit kalau push ke GitHub publik)
│   └── requirements.txt
└── frontend/        # React + Tailwind + Framer Motion
    ├── src/
    │   ├── App.js
    │   ├── components/  # Navbar, Hero, Timeline, Services, dll.
    │   └── data/        # milestones.js, site.js
    └── package.json
